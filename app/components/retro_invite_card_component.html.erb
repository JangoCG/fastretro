<%# Invite card %>
<div class="flex flex-col relative group">
  <div class="relative flex flex-col bg-[#f4f7fb] border border-zinc-900/10 shadow-lift overflow-hidden rounded-card">

    <div class="px-5 py-4 border-b border-zinc-900/10 bg-white relative overflow-hidden">
      <div class="relative z-10">
        <h2 class="font-black text-lg uppercase text-zinc-900 leading-[0.85] filter drop-shadow-sm">
          <span class="relative px-1 inline-block">
             <span class="absolute inset-0 bg-emerald-300 -rotate-1 skew-x-3 scale-110 -z-10 mix-blend-multiply opacity-80"></span>
             <%= t("components.retro_invite_card.heading") %>
          </span>
        </h2>
      </div>
    </div>

    <div class="p-4 bg-[#ffffff] relative">
      <div class="relative z-10">
        <p class="text-xs text-zinc-600 mb-3 uppercase tracking-wide font-bold">
          <%= t("components.retro_invite_card.description") %>
        </p>

        <%# Link Input with Copy Button %>
        <div class="flex gap-2" data-controller="clipboard">
          <div class="flex-1 relative">
            <input
              type="text"
              value="<%= full_invite_url %>"
              readonly
              data-clipboard-target="source"
              class="w-full px-3 py-2 text-sm font-ui bg-white border-[2px] border-zinc-900 rounded-[.875rem] text-zinc-700 truncate pr-10"
            >
          </div>
          <button
            type="button"
            data-action="clipboard#copy"
            data-clipboard-target="button"
            class="px-4 py-2 bg-zinc-900 text-white text-xs font-bold uppercase tracking-wider border-[2px] border-zinc-900 rounded-pill hover:bg-zinc-700 transition-colors shadow-soft active:shadow-none"
          >
            <%= t("components.retro_invite_card.copy") %>
          </button>
        </div>
      </div>
    </div>

  </div>
</div>
