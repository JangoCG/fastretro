<!DOCTYPE html>
<html lang="en">
  <head>
    <%# === PRIMARY META TAGS === %>
    <meta charset="UTF-8">
    <title><%= content_for(:title) || "Fast Retro — Run Better Retrospectives" %></title>
    <meta name="title" content="<%= content_for(:meta_title) || content_for(:title) || "Fast Retro — Run Better Retrospectives" %>">
    <meta name="description" content="<%= content_for(:meta_description) || "Fast Retro is an open source retrospective tool for agile teams. Brainstorm, group, vote, and discuss. Real-time collaboration, no per-user fees." %>">
    <meta name="keywords" content="<%= content_for(:meta_keywords) || "retrospective, retro, agile, scrum, sprint retrospective, team retrospective, remote retrospective, online retrospective, retrospective tool, agile retrospective, scrum master, sprint review" %>">
    <meta name="author" content="Fast Retro">
    <meta name="robots" content="index, follow">
    <meta name="language" content="English">
    <meta name="revisit-after" content="7 days">
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%# === MOBILE & APP META === %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Fast Retro">
    <meta name="application-name" content="Fast Retro">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#18181b">
    <meta name="msapplication-TileColor" content="#18181b">

    <%# === CANONICAL URL === %>
    <link rel="canonical" href="<%= content_for(:canonical_url) || request.original_url.split('?').first %>">

    <%# === OPEN GRAPH / FACEBOOK === %>
    <meta property="og:type" content="<%= content_for(:og_type) || "website" %>">
    <meta property="og:url" content="<%= content_for(:canonical_url) || request.original_url.split('?').first %>">
    <meta property="og:title" content="<%= content_for(:meta_title) || content_for(:title) || "Fast Retro — Run Better Retrospectives" %>">
    <meta property="og:description" content="<%= content_for(:meta_description) || "Fast Retro is an open source retrospective tool for agile teams. Brainstorm, group, vote, and discuss. Real-time collaboration, no per-user fees." %>">
    <meta property="og:image" content="<%= content_for(:og_image) || image_url("retro-image-no-padding.png") %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:site_name" content="Fast Retro">
    <meta property="og:locale" content="en_US">

    <%# === TWITTER CARD === %>
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:url" content="<%= content_for(:canonical_url) || request.original_url.split('?').first %>">
    <meta name="twitter:title" content="<%= content_for(:meta_title) || content_for(:title) || "Fast Retro — Run Better Retrospectives" %>">
    <meta name="twitter:description" content="<%= content_for(:meta_description) || "Fast Retro is an open source retrospective tool for agile teams. Brainstorm, group, vote, and discuss. Real-time collaboration, no per-user fees." %>">
    <meta name="twitter:image" content="<%= content_for(:og_image) || image_url("retro-image-no-padding.png") %>">
    <meta name="twitter:creator" content="@fastretro">

    <%# === STRUCTURED DATA (JSON-LD) === %>
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "Fast Retro",
      "applicationCategory": "BusinessApplication",
      "operatingSystem": "Web",
      "description": "Open source retrospective tool for agile teams. Brainstorm, group, vote, and discuss in real-time.",
      "url": "https://fastretro.app",
      "image": "<%= image_url("retro-image-no-padding.png") %>",
      "screenshot": "<%= image_url("retro-image-no-padding.png") %>",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "EUR",
        "description": "Free tier with 500 items"
      },
      "aggregateRating": {
        "@type": "AggregateRating",
        "ratingValue": "5",
        "ratingCount": "1"
      },
      "author": {
        "@type": "Organization",
        "name": "Fast Retro",
        "url": "https://fastretro.app"
      }
    }
    </script>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>

    <%# === ICONS === %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/icon.png">
    <link rel="manifest" href="/manifest.json">

    <%# === STYLESHEETS & SCRIPTS === %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="<%= content_for(:body_class) || "bg-gray-50 min-h-screen" %>">
    <%= yield :header %>
    <% if flash[:alert].present? %>
      <div class="max-w-md mx-auto px-4 mb-4">
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg text-sm">
          <%= flash[:alert] %>
        </div>
      </div>
    <% end %>
    <% if flash[:notice].present? %>
      <div class="max-w-md mx-auto px-4 mb-4">
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg text-sm">
          <%= flash[:notice] %>
        </div>
      </div>
    <% end %>
    <% if content_for?(:full_width) %>
      <main>
        <%= yield %>
      </main>
    <% else %>
      <main class="max-w-md mx-auto px-4">
        <%= yield %>
      </main>
    <% end %>
    <% if content_for?(:footer) %>
      <footer class="py-12">
        <%= yield :footer %>
      </footer>
    <% end %>
  </body>
</html>
