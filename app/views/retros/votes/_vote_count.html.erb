<%# Broadcastable vote count - no user-specific data %>
<div id="<%= dom_id %>" class="flex items-center justify-center min-w-[2.5rem] h-8 px-2 bg-tint-peach border-2 border-flame text-zinc-900 font-black text-sm relative">
  <span data-vote-total><%= count %></span>
</div>
